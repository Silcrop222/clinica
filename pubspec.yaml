name: rni_app
description: "Registro Nacional de Infieles App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.4.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  provider: ^6.1.2
  cloud_firestore: ^5.4.4
  image_picker: ^1.1.2
  firebase_storage: ^12.3.4
  url_launcher: ^6.1.10
  uuid: ^4.5.1
  intl: ^0.19.0
  dio: ^5.0.3
  http: any
  google_maps_flutter: ^2.5.3
  geolocator: ^11.0.0
  cached_network_image: ^3.3.1
  shared_preferences: ^2.2.2
  lucide_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    # - assets/logo.png  # Uncomment when assets are added
